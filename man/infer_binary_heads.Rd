% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multihead.R
\name{infer_binary_heads}
\alias{infer_binary_heads}
\title{Discover the heads of a binary model family and their thresholds}
\usage{
infer_binary_heads(client, prefix = "cap_theme_", refresh = FALSE)
}
\arguments{
\item{client}{A client object from \code{\link[=infer_connect]{infer_connect()}}.}

\item{prefix}{Model id prefix of the family (default \code{"cap_theme_"}).}

\item{refresh}{Set to TRUE to bypass the session cache.}
}
\value{
A data frame with columns \code{model_id}, \code{category}, \code{threshold}.
}
\description{
Lists the models whose id starts with \code{prefix} and fetches each head's
calibrated decision threshold from its metadata. Results are cached on
the client object environment for the session.
}
\examples{
\dontrun{
client <- infer_connect("https://gate.llm-tool.org", api_key = Sys.getenv("INFER_API_KEY"))
infer_binary_heads(client)
}
}
