% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{infer_classify_df}
\alias{infer_classify_df}
\title{Classify a data frame column}
\usage{
infer_classify_df(
  df,
  client,
  text_column,
  model = NULL,
  batch_size = 50,
  threshold = NULL,
  parallel = FALSE,
  mc_samples = 0,
  ci_level = 0.95
)
}
\arguments{
\item{df}{A data frame}

\item{client}{An infer_client object}

\item{text_column}{Name of the column containing texts}

\item{model}{Optional model ID}

\item{batch_size}{Number of texts per API call (default 50)}

\item{threshold}{Override multi-label threshold (0.0-1.0)}

\item{parallel}{Use parallel GPU+CPU inference}

\item{mc_samples}{MC Dropout forward passes for confidence intervals (0=disabled)}

\item{ci_level}{Confidence interval level (default 0.95 for 95\\% CI)}
}
\value{
The data frame with added columns. When mc_samples > 0, also adds
ci_lower_* and ci_upper_* columns for each class.
}
\description{
Classify texts in a data frame column and add result columns.
}
\details{
For single-label models:
Adds columns: label, confidence, and prob_* columns for each class.

For multi-label models:
Adds columns: labels (comma-separated), label_count, threshold,
and prob_* columns for each class.
}
\examples{
\dontrun{
client <- infer_connect("https://your-server.example.com", "YOUR_API_KEY")
df <- data.frame(text = c("Good news", "Bad news", "Neutral"))

# Single-label classification
df_classified <- infer_classify_df(df, client, "text")

# With 95\% confidence intervals (30 MC Dropout passes)
df_classified <- infer_classify_df(df, client, "text", mc_samples = 30)

# Multi-label classification with custom threshold
df_classified <- infer_classify_df(df, client, "text", model = "themes", threshold = 0.3)
}
}
