% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multihead.R
\name{infer_binary_family}
\alias{infer_binary_family}
\title{Classify texts against every head of a binary model family}
\usage{
infer_binary_family(
  client,
  texts,
  prefix = "cap_theme_",
  heads = NULL,
  chunk_size = 64L,
  max_concurrent = 8L
)
}
\arguments{
\item{client}{A client object from \code{\link[=infer_connect]{infer_connect()}}.}

\item{texts}{Character vector of texts to classify.}

\item{prefix}{Model id prefix of the family (default \code{"cap_theme_"}).}

\item{heads}{Optional character vector restricting to some model ids.}

\item{chunk_size}{Texts per request (default 64, measured optimum; hard
cap 100).}

\item{max_concurrent}{Max requests in flight (default 8).}
}
\value{
A data frame with columns \code{text_index}, \code{model_id}, \code{category},
\code{score} (positive-class probability) and \code{decision} (score at or
above the head's calibrated threshold).
}
\description{
Sends one request per (head, chunk) pair, all in flight concurrently
(\code{httr2::req_perform_parallel()}), applies each head's calibrated
threshold to the positive-class probability, and returns a tidy data
frame. Chunks are capped at 100 texts per request: beyond that the
server switches to a parallel engine that degrades sharply.
}
\examples{
\dontrun{
res <- infer_binary_family(client, df$title)
# Themes retained for sentence 3:
res$category[res$decision & res$text_index == 3]
}
}
