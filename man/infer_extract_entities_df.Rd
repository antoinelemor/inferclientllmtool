% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{infer_extract_entities_df}
\alias{infer_extract_entities_df}
\title{Extract entities from a data frame}
\usage{
infer_extract_entities_df(
  df,
  client,
  text_column,
  labels,
  model = "gliner",
  threshold = 0.5,
  flat_ner = TRUE
)
}
\arguments{
\item{df}{A data frame}

\item{client}{An infer_client object}

\item{text_column}{Name of the column containing texts}

\item{labels}{Character vector of entity types to extract}

\item{model}{NER model ID (default: "gliner")}

\item{threshold}{Confidence threshold (0.0-1.0, default: 0.5)}

\item{flat_ner}{Resolve overlapping entities (default: TRUE)}
}
\value{
The input data frame with added columns: entity_count, has_\if{html}{\out{<label>}}, entities_json
}
\description{
Sends all texts to the server in a single call (the server handles
batching and parallelization automatically). Adds columns for entity
count, per-label boolean flags, and a JSON entities column.
}
\examples{
\dontrun{
client <- infer_connect("http://localhost:8000", api_key = "...")
df <- data.frame(text = c("Steve Jobs founded Apple", "Paris, France"))
df2 <- infer_extract_entities_df(df, client, "text", c("person", "location"))
}
}
