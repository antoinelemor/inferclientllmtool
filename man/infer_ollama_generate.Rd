% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ollama.R
\name{infer_ollama_generate}
\alias{infer_ollama_generate}
\title{Generate text with Ollama}
\usage{
infer_ollama_generate(
  client,
  model,
  prompt,
  system = NULL,
  options = NULL,
  keep_alive = NULL
)
}
\arguments{
\item{client}{An infer_client object}

\item{model}{The model name (e.g., "llama3", "mistral", "gemma3:27b")}

\item{prompt}{The prompt to send to the model}

\item{system}{Optional system message}

\item{options}{Optional model parameters (temperature, top_p, etc.)}
}
\value{
The generated text (character string)
}
\description{
Generate text using an LLM via the server's Ollama instance.
}
\examples{
\dontrun{
client <- infer_connect("https://your-server.example.com", "YOUR_API_KEY")

# Simple generation
response <- infer_ollama_generate(client, "gemma3:27b", "What is R?")
cat(response)

# With system prompt
response <- infer_ollama_generate(client, "gemma3:27b",
  prompt = "Extract key entities from: The stock market crashed today",
  system = "You are an NLP expert. Return only a comma-separated list."
)

# With custom parameters
response <- infer_ollama_generate(client, "gemma3:27b",
  prompt = "Write a haiku about data science",
  options = list(temperature = 0.7)
)
}
}
