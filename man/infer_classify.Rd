% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{infer_classify}
\alias{infer_classify}
\title{Classify text(s)}
\usage{
infer_classify(
  client,
  texts,
  model = NULL,
  threshold = NULL,
  parallel = FALSE,
  device_mode = "both",
  mc_samples = 0,
  ci_level = 0.95
)
}
\arguments{
\item{client}{An infer_client object}

\item{texts}{A character vector of texts to classify}

\item{model}{Optional model ID (uses default if not specified)}

\item{threshold}{Override multi-label threshold (0.0-1.0)}

\item{parallel}{Use parallel GPU+CPU inference (for large batches)}

\item{device_mode}{Device mode for parallel: 'cpu', 'gpu', or 'both'}

\item{mc_samples}{MC Dropout forward passes for confidence intervals (0=disabled)}

\item{ci_level}{Confidence interval level (default 0.95 for 95\\% CI)}
}
\value{
A list with classification results, including training_mode and multi_label.
When mc_samples > 0, results include confidence_interval and probabilities_ci.
}
\description{
Run inference on one or more texts using a model trained with LLM Tool.
}
\details{
For multi-label models, each result contains 'labels' (list) instead of 'label'.
Use threshold to override the model's default multi-label threshold.
}
\examples{
\dontrun{
client <- infer_connect("https://your-server.example.com", "YOUR_API_KEY")

# Single text
result <- infer_classify(client, "The economy is improving")

# Multiple texts with parallel inference
results <- infer_classify(client, texts, model = "sentiment", parallel = TRUE)

# Multi-label classification with custom threshold
results <- infer_classify(client, texts, model = "themes", threshold = 0.3)

# With 95\\% confidence intervals (30 MC Dropout passes)
results <- infer_classify(client, texts, model = "sentiment", mc_samples = 30)
}
}
