% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ollama.R
\name{infer_translate}
\alias{infer_translate}
\title{Chat with Ollama}
\usage{
infer_translate(
  client,
  text,
  source_lang,
  target_lang,
  model = "translategemma:12b",
  options = NULL,
  keep_alive = NULL
)
}
\arguments{
\item{client}{An infer_client object}

\item{text}{The text to translate}

\item{source_lang}{Source language code (e.g., "en", "fr", "zh-Hans")}

\item{target_lang}{Target language code (e.g., "fr", "en", "es")}

\item{model}{TranslateGemma model variant (default: "translategemma:12b")}

\item{options}{Optional model parameters (temperature, top_p, etc.)}

\item{messages}{A list of message objects with role and content}
}
\value{
A list with the assistant's response and updated messages

A list with translation, source_lang, target_lang, model, etc.
}
\description{
Have a multi-turn conversation with an LLM via the server's Ollama instance.
}
\examples{
\dontrun{
client <- infer_connect("https://your-server.example.com", "YOUR_API_KEY")

# English to French
result <- infer_translate(client, "Hello, how are you?", "en", "fr")
cat(result$translation)

# French to Spanish
result <- infer_translate(client, "Bonjour le monde", "fr", "es")

# With custom parameters
result <- infer_translate(client, "The economy is growing", "en", "de",
  options = list(temperature = 0.3)
)
}
}
