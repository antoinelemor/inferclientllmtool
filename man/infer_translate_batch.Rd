% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ollama.R
\name{infer_translate_batch}
\alias{infer_translate_batch}
\title{Translate a batch of texts with TranslateGemma (server-side pacing)}
\usage{
infer_translate_batch(
  client,
  texts,
  source_lang,
  target_lang,
  model = "translategemma:12b",
  options = NULL,
  delay_ms = 200,
  chunk_size = 8
)
}
\arguments{
\item{client}{An infer_client object}

\item{texts}{Character vector of texts to translate}

\item{source_lang}{Source language code (e.g., "en", "fr")}

\item{target_lang}{Target language code (e.g., "fr", "en")}

\item{model}{TranslateGemma model variant (default: "translategemma:12b")}

\item{options}{Optional model parameters (temperature, top_p, etc.)}

\item{delay_ms}{Delay between Ollama calls in ms (default: 200)}

\item{chunk_size}{Max texts per HTTP call (default: 8)}
}
\value{
A list with translations (character vector), source_lang,
target_lang, model, count, errors
}
\description{
Texts are automatically chunked into groups of \code{chunk_size} to stay
within proxy timeout limits (e.g. Cloudflare 100s). Each chunk is sent
as a separate HTTP request; the server translates texts sequentially
with a configurable delay between Ollama calls.
}
\examples{
\dontrun{
client <- infer_connect("https://your-server.example.com", "YOUR_API_KEY")

result <- infer_translate_batch(client,
  c("Hello world", "Good morning", "Thank you"),
  "en", "fr"
)
cat(result$translations)
}
}
